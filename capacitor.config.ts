import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.atabs.myspendingtracker',
  appName: 'Spending Tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
